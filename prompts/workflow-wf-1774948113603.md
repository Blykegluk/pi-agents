---
description: "Video publication Yourbodyexplained — generate and publish videos daily"
---
Use the subagent tool with the chain parameter to execute this workflow:

1. Use the "Script-creator" agent to execute: $@
2. Then, use the "video-generator" agent to process the output from the previous step (use {previous} placeholder)
3. Then, use the "video-editor" agent to process the output from the previous step (use {previous} placeholder)
4. Then, use the "youtube-publisher" agent to process the output from the previous step (use {previous} placeholder)
5. Then, use the "tiktok-publisher" agent to process the output from the previous step (use {previous} placeholder)

Execute this as a chain, passing output between steps via {previous}.
